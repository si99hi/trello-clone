{
  "name": "taskflow-backend",
  "version": "1.0.0",
  "description": "Backend for Taskflow Trello clone",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "npx prisma db push && npx prisma generate && node prisma/seed.js && node src/server.js",
    "migrate": "prisma migrate dev",
    "seed": "node prisma/seed.js",
    "postinstall": "npx prisma generate",
    "prisma:push": "npx prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^8.0.2",
    "resend": "^6.9.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "prisma": "^5.0.0"
  }
}
